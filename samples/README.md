# Demo samples

- `demo-checklist.md` – short walkthrough script (good for screenshares)
- `requests.http` – URLs to open/hit once the server is running
- `copilot-prompts.md` – prompt ideas to extend the project with Copilot
