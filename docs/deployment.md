# Deployment

This project is portfolio-focused. The recommended "live demo" path is GitHub Codespaces so reviewers can run the full Rails environment without local setup.

If you do deploy to a host, ensure:
- No secrets are committed
- DB is provisioned
- Assets are built
- A11y smoke tests run in CI
