# Styling

This repo uses TailwindCSS for consistent spacing, typography, and focus styles.

Principles:
- Prefer utility classes over custom CSS
- Keep contrast high (check WCAG contrast ratios)
- Respect user preferences (reduced motion, etc.)
