# Components

This project uses **ViewComponent** to build reusable, testable UI primitives.

## How to browse components
- Start the Rails server
- Open the previews at `/rails/view_components`

## Conventions
- Keep components small and focused
- Prefer semantic HTML first; add ARIA only when needed
- Include keyboard interaction support for any custom UI
